---
permalink: /search-index.json
---[
{% for item in collections.doc %}
  {
    "url": "{{ item.url }}",
    "title": "{{ item.data.title | default(item.fileSlug) }}",
    "content": {{ item.templateContent | striptags | dump | safe }}
  }{% if not loop.last %},{% endif %}
{% endfor %}
]