{% extends "base.njk" %}

{% block content %}
<h1>{{ title }}</h1>

{% if key %}
<div class="alert alert-info" role="alert">
  Available from {{ key | tick | md | safe }}.
</div>
{% endif %}

{{ content | safe }}
{% endblock %}
